// Tailwind v4 is a single PostCSS plugin — no tailwind.config.js, no autoprefixer.
// Theme tokens and content sources are declared in CSS instead, see
// src/app/globals.css.
export default {
	plugins: {
		'@tailwindcss/postcss': {},
	},
}
